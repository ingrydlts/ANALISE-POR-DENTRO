# Faixas alinhadas entre os 4 workflows (coleta, concorrência, audiência, tarefas) —
# unificado aqui para que todos instalem sempre a mesma combinação testada.
#
# anthropic + httpx: fixado para evitar o bug conhecido "Client.__init__() got an
# unexpected keyword argument 'proxies'", causado por httpx>=0.28 junto de versões
# do anthropic que ainda passam 'proxies' internamente.
anthropic>=0.39,<1.0
httpx>=0.27,<0.28

# notion-client: >=2.7.0 é o piso real para a camada de "data sources" (API Notion
# 2025-09-03+) usada por todos os scripts (data_sources.query / databases.retrieve
# ->data_sources). Versões mais antigas (ex.: 2.2.1) não têm esse recurso.
notion-client>=2.7.0,<3.0

python-dotenv>=1.0.1,<2.0
google-api-python-client==2.126.0

# create_tasks.py (Módulo 7 — Execução de Insights) fala com a API do Notion via HTTP direto.
requests>=2.31,<3.0

# enriquecer_conversas_audiencia.py: redimensiona screenshots antes de mandar
# pra Claude, pra evitar 413 "Request Too Large" com imagens em alta resolução.
Pillow>=10.0,<12.0
